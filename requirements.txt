# Construction Equipment Weekly — Python Dependencies
# Install: pip install -r requirements.txt

feedparser==6.0.11        # RSS/Atom feed parsing
requests==2.31.0          # HTTP requests with timeout/retry
beautifulsoup4==4.12.3    # HTML content extraction
lxml==5.2.2               # Fast XML/HTML parser (bs4 backend)
